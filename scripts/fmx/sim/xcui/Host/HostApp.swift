// focusmaxxing hub simulator harness: the do-nothing host a ui-test bundle has to have.
import SwiftUI

@main
struct HostApp: App {
    var body: some Scene {
        WindowGroup {
            Text("sim harness host")
        }
    }
}
